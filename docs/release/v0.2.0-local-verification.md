# dsh-tui v0.2.0 local verification

Date: 2026-08-22

Verification target: local `codex/dsh-tui-v0.2` release-candidate worktree,
built on commit `ddb359b` plus the version and documentation changes recorded
with this file.

## Environment

| Item | Value |
| --- | --- |
| OS | macOS 26.3 (25D125) |
| Node.js | v26.4.0 |
| npm | 11.17.0 |
| Package | `@nico0713520/dsh-tui@0.2.0` |
| DSH compatibility target | `0.1.1-rc.2` |

The package supports Node.js `^22.19.0 || >=24.0.0`; the local run used a
newer matching Node version. Cross-platform CI results are not inferred from
this local record.

## Final command results

| Command | Result | Evidence |
| --- | --- | --- |
| `npm run lint` | PASS | oxlint completed with warnings denied |
| `npm run typecheck` | PASS | `tsc --noEmit` |
| `npm run test:unit` | PASS | 38 files, 195 tests |
| `npm run test:integration` | PASS | 1 file, 17 tests |
| `npm run test:pty` | PASS | 3 files, 19 tests |
| `npm run composition:check` | PASS | Darwin composition loaded and exercised |
| `npm run compat:check` | PASS | all direct DSH packages matched `0.1.1-rc.2` |
| `npm run build` | PASS | Node 22.19 target bundle built |
| `npm run publint` | PASS | no package warnings |
| `npm run pack:check` | PASS | allowlist passed with 14 files |
| `npm run install:check` | PASS | packed install reported `dsh-tui 0.2.0` and completed Echo TTY smoke |

The full source suite run immediately before the final sequence reported 42
passing test files, 231 passing tests, and one opt-in live test skipped.

## Stress and resilience coverage

- 10,000 live text chunks and 100 unique tool lifecycles completed through the
  standard ACP process boundary.
- The same session survived 48×16, 120×30, 32×14, and 80×24 resize changes,
  then accepted another prompt.
- Deliberately delayed chunks kept one queued follow-up editable and delivered
  it after the active prompt.
- A post-prompt live barrier prevents late tool events from producing an
  incorrect turn summary.
- Approval allow/reject/Escape, cancellation, unknown outcome, history replay,
  new session, no-color, DeepSeek Light, and inline-image paths passed real PTY
  tests.

## Live DeepSeek acceptance

SKIPPED: `DEEPSEEK_API_KEY` was not present in the command environment. No
credential was requested, copied from chat, read from managed storage, or
placed in command arguments. The opt-in test remains available as:

```bash
DSH_LIVE=1 npm run test -- tests/live/dsh-live.test.ts
```

## Known limits

- Subjective whale sharpness and color balance still require owner review in
  the owner's preferred terminal/font; deterministic visual checks are recorded
  separately in `v0.2.0-visual-verification.md`.
- History replay is read-only; the client does not claim ACP resume support.
- The local result does not claim a GitHub Actions run, npm publication, tag,
  release, or remote branch update.
